<container>
  <vbox>
    <hbox>
      <centered>{{model.firstName}}</centered>
    </hbox>
    {{song-row name="name" key="key" genre="genre"}}
    {{#each model.songs as |song|}}
      {{song-row name=song.name key=song.key genre=song.genre}}
    {{/each}}
  </vbox>
  {{outlet}}
</container>
